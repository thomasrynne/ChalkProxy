ROOT=`dirname $0`
if [ -z $JAVA_HOME ]; then
  echo "JAVA_HOME must be set"
  exit 1
fi
if [ -z $SCALA_HOME ]; then
  echo "SCALA_HOME must be set"
  exit 1
fi
cd $ROOT
$JAVA_HOME/bin/java -cp out:$SCALA_HOME/lib/scala-library.jar:lib/* chalkproxy.Run $@